import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mightystarfishsnap',
  appName: 'mighty-starfish-snap',
  webDir: 'dist'
};

export default config;
